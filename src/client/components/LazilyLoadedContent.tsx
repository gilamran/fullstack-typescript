import Typography from '@mui/material/Typography';
import React from 'react';

const LazilyLoadedContent: React.FC = () => <Typography>This component was loaded lazily from a seperate chunk</Typography>;

export default LazilyLoadedContent;
